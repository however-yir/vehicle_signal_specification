# Reports Directory

This directory stores generated governance reports, for example:

- schema diff reports (`schema-diff*.md`)
- quarterly upstream sync reports (`upstream-sync-*.md`)

Reports are typically generated by scripts and CI workflows.
